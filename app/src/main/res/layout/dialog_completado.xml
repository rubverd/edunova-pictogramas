<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="24dp"
    android:gravity="center_horizontal"
    android:background="@drawable/dialog_background"> <!-- Opcional: si creaste el fondo con bordes redondeados -->

    <ImageView
        android:id="@+id/icono_tick"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:src="@drawable/ic_check_circle"
        app:tint="@color/verde_correcto"
        android:contentDescription="Icono de completado" />

    <TextView
        android:id="@+id/mensaje_completado"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="¡Felicidades, has completado todas las sílabas!"
        android:textSize="20sp"
        android:textStyle="bold"
        android:textColor="@android:color/black"
        android:textAlignment="center" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_marginTop="24dp">

        <Button
            android:id="@+id/boton_repetir"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Repetir Actividad"
            app:backgroundTint="@color/rojoBoton" /> <!-- Asegúrate de que este color exista en colors.xml -->

        <Button
            android:id="@+id/boton_volver_menu"
            style="?attr/materialButtonOutlinedStyle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="Volver al Menú"
            android:textColor="@color/rojoBoton"
            app:strokeColor="@color/rojoBoton" /> <!-- Asegúrate de que este color exista en colors.xml -->

    </LinearLayout>

</LinearLayout>
